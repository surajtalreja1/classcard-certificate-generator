{
  "name": "classcard-certificate-generator",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.2.0",
    "astro": "^5.6.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "wrangler": "^3.105.0"
  }
}
